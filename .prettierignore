# Generated by `wrangler types` (npm run cf-typegen) -- 14,682 lines of runtime
# type declarations. Prettier rewrites about 25,000 lines of it, and cf-typegen
# writes the generator's formatting straight back on the next run, so a
# formatted copy survives exactly until someone regenerates types and then
# turns format:check red on main with no code change to explain it.
#
# It stays in tsconfig.json's `include` -- the Worker's Env type comes from it.
# Ignoring it for FORMATTING is a separate question from typechecking it.
worker-configuration.d.ts

# npm owns this.
package-lock.json

# Local Wrangler state.
.wrangler/

# Local agent state, untracked. Present only on a developer's machine, so
# formatting it would fail check runs for some people and not others.
.claude/
